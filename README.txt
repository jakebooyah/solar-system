README

Simple double click on solarSystemGRA.xcodeproj to open the project
in Xcode. Using the shortcut key "cmd + r" will build and run the
application. Alternatively this can be done by clicking on Run under the
Project tab.